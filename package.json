{
  "name": "leo-mart",
  "version": "1.0.0",
  "private": true,
  "description": "Leo Mart - Nepali Grocery Marketplace",
  "scripts": {
    "dev": "concurrently --kill-others --names backend,frontend --prefix-colors green,blue \"npm run dev:backend\" \"wait-on http://localhost:5000 && npm run dev:frontend\"",
    "dev:backend": "cd leo_mart_backend && npm run dev",
    "dev:frontend": "cd leo_mart_frontend/leo-mart && npm run dev",
    "seed": "cd leo_mart_backend && npm run seed",
    "build:backend": "cd leo_mart_backend && npm run build",
    "build:frontend": "cd leo_mart_frontend/leo-mart && npm run build",
    "install:all": "cd leo_mart_backend && npm install && cd ../leo_mart_frontend/leo-mart && npm install"
  },
  "devDependencies": {
    "concurrently": "^10.0.4",
    "wait-on": "^9.1.0"
  }
}
